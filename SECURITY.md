# 安全策略

## 受支持版本

| 版本 | 支持状态 |
| ---- | -------- |
| main | ✅ 积极维护 |

## 报告漏洞

请 **不要** 公开 GitHub Issue 提交安全漏洞。请直接发送邮件至:

- **邮箱**: tyza66@outlook.com(或通过 FasterEdge 组织主页联系)

请在邮件中提供:

- 漏洞描述与影响面
- 复现步骤(尽量最小化)
- 受影响版本与补丁建议

我们承诺:

- 收到报告后 **72 小时内** 确认并回复;
- 修复期间对漏洞细节保密;
- 修复发布后公开致谢(若您同意)。

## 安全设计基线(与 DontCrack 家族一致)

- **fail-closed**: 配置非法(重复端口、依赖环、外部监听缺密码等)一律拒绝启动;
- **最小权限**: 聚合状态 HTTP 默认仅监听 127.0.0.1;对外监听必须配置密码,
  否则直接拒绝启动;
- **常数时间鉴权**: 密码比较使用 `crypto/subtle.ConstantTimeCompare`,避免时序侧信道;
- **超时保护**: 子进程(含 DontCrack 实例)健康查询、依赖等待、优雅停机均有超时上限;
- **无第三方远程执行面**: 本工具仅管理本地 DontCrack 进程,不引入远程执行入口。